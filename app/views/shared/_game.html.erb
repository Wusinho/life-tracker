<%= form_with(model: Game.new ) do |f| %>
  <div class="field">
    <%= f.label 'House name' %><br />
    <%= f.text_field :name, autocomplete: "new-name" %>
  </div>

  <div class="actions">
    <%= f.submit "House up", class: blue_btn %>
  </div>
<% end %>